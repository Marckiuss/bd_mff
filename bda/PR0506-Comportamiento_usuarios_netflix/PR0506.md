# PR0506. Análisis de comportamiento de usuarios en Netflix
Contexto
En esta práctica vamos a trabajar con el dataset Netflix audience behaviour - IK Movies que abarca el comportamiento en la versión web de los usuarios en Netflix UK entre enero de 2017 y junio de 2019 de forma anonimizada. Este dataset documenta cada vez que alguien hizo un click en una URL del tipo netflix.com/watch para ver una película.

Los campos que tiene el dataset son:

**row_id**: identificador de fila

**click_datetime**: fecha y hora del clic

**time_to_next_click**: segundos hasta el siguiente clic del usuario

**movie_title**: título de la película

**movie_genres**: género/s

**release_date**: fecha de estreno original

**title_id**: ID de la película

**user_id**: ID del usuario

Tu objetivo será utilizar funciones de ventana en PySpark para extraer insights sobre cómo los usuarios británicos navegan y consumen películas en la interfaz web de Netflix.com.


```python
from pyspark.sql import SparkSession
from pyspark.sql.types import StructType, StructField, StringType, DoubleType, IntegerType

# Crear la sesión de Spark
spark = SparkSession.builder \
    .appName("PR0506 - Comportamiento Usuarios Netflix") \
    .getOrCreate()
```

    Setting default log level to "WARN".
    To adjust logging level use sc.setLogLevel(newLevel). For SparkR, use setLogLevel(newLevel).
    26/03/09 23:50:00 WARN NativeCodeLoader: Unable to load native-hadoop library for your platform... using builtin-java classes where applicable
    26/03/09 23:50:01 WARN Utils: Service 'SparkUI' could not bind on port 4040. Attempting port 4041.



```python
from pyspark.sql.types import StructType, StructField, StringType, DoubleType, TimestampType, IntegerType, TimestampType

# Datos para la predicción del rendimiento en cultivos
schema_netflix = StructType([
    StructField("row_id", IntegerType(), True),
    StructField("click_datetime", TimestampType(), True),
    StructField("time_to_next_click", DoubleType(), True),
    StructField("movie_title", StringType(), True),
    StructField("movie_genres", StringType(), True),
    StructField("release_date", DateType(), True),
    StructField("title_id", StringType(), True),
    StructField("user_id", StringType(), True)
])

df_netflix = spark.read.format("csv") \
    .option("header", "true") \
    .schema(schema_netflix) \
    .load("data/vodclickstream_uk_movies_03.csv")

df_netflix.printSchema()
df_netflix.show(5)
```


    ---------------------------------------------------------------------------

    NameError                                 Traceback (most recent call last)

    Cell In[2], line 10
          1 from pyspark.sql.types import StructType, StructField, StringType, DoubleType, TimestampType, IntegerType, TimestampType
          3 # Datos para la predicción del rendimiento en cultivos
          4 schema_netflix = StructType([
          5     StructField("row_id", IntegerType(), True),
          6     StructField("click_datetime", TimestampType(), True),
          7     StructField("time_to_next_click", DoubleType(), True),
          8     StructField("movie_title", StringType(), True),
          9     StructField("movie_genres", StringType(), True),
    ---> 10     StructField("release_date", DateType(), True),
         11     StructField("title_id", StringType(), True),
         12     StructField("user_id", StringType(), True)
         13 ])
         15 df_netflix = spark.read.format("csv") \
         16     .option("header", "true") \
         17     .schema(schema_netflix) \
         18     .load("data/vodclickstream_uk_movies_03.csv")
         20 df_netflix.printSchema()


    NameError: name 'DateType' is not defined


## 1.- Auditoría de telemetría Web (validación de datos)
Supón que el equipo de ingeniería web necesita verificar si el rastreador del navegador calculó correctamente el tiempo entre clics.

Ignora la columna **time_to_next_click** original. Crea una nueva columna **calculated_time_to_next** calculando tú mismo la diferencia en segundos entre el **click_datetime** de la fila actual y el del siguiente registro cronológico del mismo usuario (recuerda que puedes usar la función **lead()** para extraer información de una columna anterior).

Compara ambas columnas para ver si hay discrepancias.


```python
from pyspark.sql.window import Window
import pyspark.sql.functions as F

# 1. Definimos la ventana
ventana_usuario = Window.partitionBy("user_id").orderBy("click_datetime")

# 2. Obtenemos el tiempo del siguiente clic usando la función lead()
df_netflix = df_netflix.withColumn("next_click_datetime", F.lead("click_datetime").over(ventana_usuario))

df_netflix = df_netflix.withColumn("calculated_time_to_next",
    F.unix_timestamp("next_click_datetime") - F.unix_timestamp("click_datetime")
)

df_netflixFiltered = df_netflix.filter(F.col("calculated_time_to_next") != F.col("time_to_next_click"))

df_netflixFiltered.select("user_id", "time_to_next_click", "calculated_time_to_next").show()
```

## 2.- Detección de “zapping”
En la versión web, es muy común que los usuarios hagan clic en una película, vean los primeros segundos y vuelvan atrás si no les convence.

Identifica estos rechazos rápidos. Utiliza la función **lead()** (o **lag()**) para calcular el tiempo de visualización. Si el tiempo hasta el siguiente clic del usuario en otra película es inferior a 5 minutos (300 segundos), crea una columna llamada **es_zapping** y márcala con un **1** (de lo contrario, **0**).


```python
df_zapping = (df_netflix.withColumn("es_zapping",F.when(F.col("calculated_time_to_next") < 300, 1).otherwise("0")))

df_zapping.show(5)
```

## 3.- El ranking de “maratones”
Queremos saber cuántas películas llega a iniciar un usuario web en un solo día.

Extrae solo la fecha (sin la hora) de la columna **click_datetime**.

Utiliza **row_number()** particionando por **user_id** y por la **fecha**, ordenando por **click_datetime**. Esto creará un contador diario (pelicula_nro_1, pelicula_nro_2…). 

Filtra el DataFrame final para encontrar a los usuarios extremos que hayan iniciado más de 5 películas en un mismo día.


```python

df_maraton = (df_netflix.withColumn("fecha_peli",F.split(F.col("click_datetime")," ").getItem(0)))

ventana_maraton = Window.partitionBy("user_id","fecha_peli").orderBy("fecha_peli")

df_maraton = df_maraton.withColumn("contador_pelis",F.row_number().over(ventana_maraton))

df_maraton = df_maraton.filter(F.col("contador_pelis") > 5 )

df_maraton.show(10)
```

## 4.- Análisis de re-visualización
Queremos identificar las películas que los usuarios del Reino Unido ven repetidas veces en sus ordenadores.

Crea una columna llamada **veces_vista_por_usuario** que cuente cuántas veces un mismo **user_id** ha hecho clic en el mismo **title_id** a lo largo de todo el periodo (2017-2019). Usa la función **count()** sobre una ventana particionada por usuario y película. Luego, filtra para mostrar solo aquellos registros donde el usuario haya visto la misma película 3 veces o más.


```python
ventana_repetidas = Window.partitionBy("user_id","title_id")

df_repetidas = (df_netflix.withColumn("veces_vista_por_usuario", F.count("title_id").over(ventana_repetidas)))

df_repetidas = df_repetidas.filter(F.col("veces_vista_por_usuario") > 3)

df_repetidas.show()
```

    26/03/09 23:50:20 WARN GarbageCollectionMetrics: To enable non-built-in garbage collector(s) List(G1 Concurrent GC), users should configure it(them) to spark.eventLog.gcMetrics.youngGenerationGarbageCollectors or spark.eventLog.gcMetrics.oldGenerationGarbageCollectors

